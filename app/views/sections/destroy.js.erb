//window.location.reload();
$("#<%= @section.id %>-section-and-items").fadeOut(300, function(){
  $(this).remove();
});

<% @items.each do |item| %>
  $('.item_<%= item.id %>_dropdown option[value="<%= @section.id%>"]').remove()
<% end %>

