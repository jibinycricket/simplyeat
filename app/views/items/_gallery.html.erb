<% @items.each do |item| %>
<div class="item-image">
  <% picId = item.name + ' image' %>
  <%= link_to( image_tag(item.item_picture.small_pic), image_url(item.item_picture.medium_pic), class: "fancybox", id: picId, rel: "gallery1", title: item.name) %>
  </div>
<% end %>
 

